server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: physicsplay-backend
  datasource:
    # 1. Copiado tal cual de tu captura: El subdominio correcto es aws-1-sa-east-1
    url: ${SUPABASE_DB_URL:jdbc:postgresql://aws-1-sa-east-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0}
    
    # 2. Copiado tal cual de tu captura: El usuario oficial que te da Supabase es este sin agregarle nada más
    username: ${SUPABASE_DB_USER:postgres.tzolgewtynjwdbipsamt}
    
    # 3. Tu contraseña asignada
    password: ${SUPABASE_DB_PASSWORD:PhysicsPlay2026.}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    postgresql:
      transactional: false 
    baseline-on-migrate: true
    baseline-version: 0
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
  supabase:
    api-key: ${SUPABASE_API_KEY:sb_publishable_uZGr2GzAA-zkibaLFP2g8g_KbAZ8xzT}
  google-ai:
    api-key: ${GOOGLE_AI_STUDIO_API_KEY:AIzaSyAYhhbRJ-TKmqnzPu-WnihcLKJe1FK43TE}
    model: ${GOOGLE_AI_MODEL:gemini-1.5-flash}
